import java.util.*;

public class Elephant {
	// rows and columns
	static int R, C;

	public static void main(String[] args) {
		// Scanner to read from standard input
		Scanner scan = new Scanner(System.in);
		// Reads the next two integers (skips spaces and newlines automatically)
		R = scan.nextInt();
		C = scan.nextInt();
		// Reads the next line as a String
		// String s = scan.next();

		// Print the result
		System.out.println(42);
	}
}
